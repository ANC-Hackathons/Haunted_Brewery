'use strict';

describe('myApp.addPlayer module', function() {

  beforeEach(module('myApp.addPlayer'));

  describe('addPlayer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addPlayerCtrl = $controller('addPlayerCtrl');
      expect(addPlayerCtrl).toBeDefined();
    }));

  });
});
