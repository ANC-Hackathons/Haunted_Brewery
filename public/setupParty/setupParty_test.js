'use strict';

describe('myApp.setupParty module', function() {

  beforeEach(module('myApp.setupParty'));

  describe('setupParty controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SetupPartyCtrl = $controlSetupPartyCtrl('View1Ctrl');
      expect(SetupPartyCtrl).toBeDefined();
    }));

  });
});
